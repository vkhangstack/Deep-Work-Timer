output: dist/
releases:
  - name: dev
    jobs:
      - name: deep-work-timer
        package:
          platform: linux
          target: deb
          build_args:
            enable-experiment: records
